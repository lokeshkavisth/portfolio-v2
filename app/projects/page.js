import { ProjectsContent } from "@/components/ProjectsContent";

export const metadata = {
  title: "Projects",
  description:
    "Explore web applications and full-stack projects built with the MERN stack — from AI platforms to Chrome extensions and developer tools.",
};

export default function Projects() {
  return <ProjectsContent />;
}
